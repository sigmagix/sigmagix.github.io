const menuBtn=document.querySelector('.menu-btn');const navLinks=document.querySelector('.nav-links');if(menuBtn){menuBtn.addEventListener('click',()=>{const open=navLinks.classList.toggle('open');menuBtn.setAttribute('aria-expanded',String(open))});navLinks.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{navLinks.classList.remove('open');menuBtn.setAttribute('aria-expanded','false')}))}
const dialog=document.querySelector('.video-dialog');const video=dialog?.querySelector('video');const title=dialog?.querySelector('h2');document.querySelectorAll('[data-video]').forEach(btn=>btn.addEventListener('click',()=>{video.src=btn.dataset.video;video.poster=btn.dataset.poster||'';title.textContent=btn.dataset.title;dialog.classList.toggle('portrait',btn.dataset.orientation==='portrait');dialog.showModal()}));function closeVideo(){video.pause();video.removeAttribute('src');video.load();dialog.close()}dialog?.querySelector('.dialog-close').addEventListener('click',closeVideo);dialog?.addEventListener('click',e=>{if(e.target===dialog)closeVideo()});document.addEventListener('keydown',e=>{if(e.key==='Escape'&&dialog?.open)closeVideo()});
